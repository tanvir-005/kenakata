#KenaKata

## Getting Started

Install dependencies and create your local environment file:

```bash
cp .env.example .env.local
npm install
```

Run the development server:

```bash
npm run dev
```

you can run some basic tests by running:

```bash
bash scripts/test.sh
```